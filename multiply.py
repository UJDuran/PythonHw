def multiply_list():
    numList = []
    inputList = input()
    inputList = inputList.split()
    
    product = 1
    
    for i in inputList:
        if i.isdigit
        numList.append(i)
    else:
        return bool(0)
    
    for i in numList:
        product = product * i
    return product
